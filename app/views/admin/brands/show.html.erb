<section class="content" data-controller="admin--brand">
  <div class="container-fluid">
    <%= render "shared/notice" %>

    <div class="row">
      <div class="col-md-12">
        <div class="float-right">
          <%= link_to admin_brands_path, class: 'btn btn-info' do %>
            <i class="fas fa-list"></i>
          <% end %>
          <%= link_to edit_admin_brand_path(@brand), class: 'btn btn-warning' do %>
            <i class="far fa-edit"></i>
          <% end %>
          <%= link_to [:admin, @brand], data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' }, class: 'btn btn-danger' do %>
            <i class="fas fa-trash"></i>
          <% end %>
        </div>
      </div>

      <div class="col-md-12">
        <div class="card card-primary">
          <div class="card-body">
            <div class="row">
              <div class="col-sm-6">
                <div class="form-group">
                  <label>Name</label>
                  <input type="text" class="form-control" disabled="" value="<%= @brand.name %>">
                </div>
              </div>

              <div class="col-sm-6">
                <div class="form-group">
                  <label>Status</label>
                  <input type="text" class="form-control" disabled="" value="<%= @brand.status %>">
                </div>
              </div>

            <% @brand.custom_fields.each do |cf| %>
              <div class="col-sm-6">
                <div class="form-group">
                  <label><%= cf.name %></label>
                  <input type="text" class="form-control" disabled="" value="<%= cf.value %>">
                </div>
              </div>
            <% end %>
            </div>
          </div>
        </div>
      </div>

    </div>
  </div>
</section>
